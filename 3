#include <stdio.h>
#include <string.h>

char a[15] = "I am a student.";
char b[15];
int size;

int main()
{
  size = strlen(a);
  for(int i = 0; i<=size; i++)
    {
      b[size-i] = a[i];
    }
  for(int i = 0; i<=size; i++)
    {
      printf("%c",b[i]);
    }
}
